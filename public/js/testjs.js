
 // --------for number 1----------(i didnt have enought time)// 
 // var number = (-1) // true
 // var number = (6) // false

function isNegative () {
	if (input) < 0 {
		return true;
	} else {
		return false;
	}
}



// --------for number 2----------//
average([1, 2, 3]) // 2
average([4, 6, 8, 10, 12]) // 8
function numSum (total, num) {
	return total + num;
}
function average (item) {
		values.reduce(numSum)
}

console.log (average([1, 2, 3]))
console.log (average([28,34]))

// --------for number 3----------//
 var = values([1, 2, 3]) // 2
// values([4, 6, 8, 10]) // 0
// values([1, 2, 1, 1]) // 3
var i = 0
function countOdds() {
	if (values) {

	}
}
console.log (average([1, 2, 3]);

// --------for number 4----------//
// Write a function named convertNameToObject that accepts a string that contains a first name and last name separated by a space, and returns an object with properties firstName and lastName.

convertNameToObject('Harry Potter'); // {firstName: 'Harry', lastName: 'Potter'}
convertNameToObject('Ron Weasley') // {firstName: 'Ron', lastName: 'Weasley'}


function convertNameToObject(wholeName) {
	var chop = wholeName.split(" ");
	var names = {
		firstName: chop[0],
		lastName: chop [1]
	}
	return names;	
}

console.log (convertNameToObject('Ron Weasley'));

// --------for number 5----------//


var names = [
     {firstName: 'Harry', lastName: 'Potter'},
     {firstName: 'Ron', lastName: 'weasley'},
     {firstName: 'Hermione', lastName: 'granger'}
 ];
 upperCaseLastNames(names);
 /*
 [
    {firstName: 'Harry', lastName: 'Potter'},
    {firstName: 'Ron', lastName: 'Weasley'},
    {firstName: 'Hermione', lastName: 'Granger'}
 ]
  */

function changeDesc( value, desc ) {
   for (var i in projects) {
     if (projects[i].value == value) {
        projects[i].desc = desc;
        break; //Stop this loop, we found it!
     }
   }
}

console.log upperCaseLastNames(names));






